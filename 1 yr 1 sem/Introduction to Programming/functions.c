#include <stdio.h>

int acc(int acc);
char plan(char plan);

int main()
{

  return EXIT_SUCCESS;
}
int acc(int acc){
  printf("What is your account number?");
  scanf("%i",&acc);
}
char plan(char plan){
  printf("Is your billing plan commercial(C), industrial(I) or residential(R)\n")
}
