Event handling refers to the mechanism where events occur and the program executes appropriate responses.
1. The source - the object that creates events i.e a button creates an ActionEvent
2. Event object - information about the event ActionEvent, MouseEvent or KeyEvent
3. Event Listener - a class or interface which handles events.
4. Event handler- the implementation of the listener's method that defines what should happen when the event occurs