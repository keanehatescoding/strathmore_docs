Works on a single relation and defines a relation that contains tuples that satisfy a given predicate.
It is also called restriction

## Relational Algebra Equivalent Expression

$$\sigma_{\text{predicate}}\text{tblName}$$

## Oracle PL/SQL Equivalent Expression

```SQL
SELECT * FROM tblName 
WHERE Predicate
```