__final__ is a special keyword in java that have different meanings at runtime. 
The final keyword can be used to create constant attributes ( attributes that can be changed).
The final keyword can also be used to create classes that cannot be extended(inherited)
e.g.
```java
final class CannotBeExtended {
}
```
records and enums in java are compiled as final classes.