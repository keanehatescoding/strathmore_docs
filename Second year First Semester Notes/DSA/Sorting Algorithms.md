## Types of sorting Algorithms
1. Bubblesort
2. Quicksort
3. Insertion sort
4. Selection sort
5. Merge sort
6. Heap sort
7. Count sort
8. Radix sort
9. Bucket sort

## 1. Bubble Sort

Time Complexity $O(n^2)$
Worst case- $O(n^2)$
Best case- $O(n)$
Space Complexity $O(1)$

```python
def buble_sort(arr):
	n = len(arr)
	while True:
		for i in range(1,n):
			if(arr[i-1]>arr[i]):
			arr[i-1],arr[i] = arr[i],arr[i-1]
```

```Cpp
#include <cstdlib>
#include <iostream>
using namespace std;

const float* bubblesort(float* arr);
int main(){
	bubblesort();
}
const float* bubblesort(float* arr){
	int i;
	arr_len = sizeof(arr) / sizeof(arr[0]);
	while True{
	for(;i<arr_len;i++){
		
	}
// to do	
```


## 2. Selection Sort

Time Complexity $O(n^2)$
Worst case - $O(n^2)$
Best case - $O(n^2)$


## 3. Insertion Sort

## 4. Merge Sort

## Merge Sort

Time Complexity - $O(n\log n)$


## Heap Sort