Works on a single column and returns a single relation that contains a vertical subset of the relation, extracting values of specific attributes and eliminating duplicates.

## Relational Algebra Operations Expression
$$\prod_{col1,col2,...,coln}\text{tblName}$$
## Oracle PL/SQL Equivalent Expression
```SQL
SELECT col1, col2, coln FROM tblName;
```
